version: 2.1

jobs:
  install-node-example:
    docker:
      - image: 'cimg/base:stable'
    steps:
      - checkout
      - node/install:
          install-yarn: true
      - run: node --version

orbs:
  node: circleci/node@10.18.1

version: 2.1
workflows:
  test_my_app:
    jobs:
      - install-node-example
