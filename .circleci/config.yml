version: 2.1

jobs:
  install-node-example:
    docker:
      - image: 'cimg/base:stable'
    steps:
      - checkout
      - node/install:
          install-yarn: true
      - run: node --version

orbs:
  node: cimg/node@10.18.1-stretch

workflows:
  test_my_app:
    jobs:
      - install-node-example
